from __future__ import annotations

"""Config service package.

Import concrete modules directly from `services.config.*` to avoid
package-init cycles between config editing and shared error mapping.
"""

__all__: tuple[str, ...] = ()
